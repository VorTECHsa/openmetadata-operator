---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: openmetadataconnections.openmetadata.vortexa.com
spec:
  group: openmetadata.vortexa.com
  names:
    kind: OpenMetadataConnection
    listKind: OpenMetadataConnectionList
    plural: openmetadataconnections
    singular: openmetadataconnection
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          OpenMetadataConnection defines how to connect to a target OpenMetadata instance.
          It is referenced by name from managed resources such as OpenMetadataService.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              OpenMetadataConnectionSpec defines the connection details for an OpenMetadata instance.
              Multiple managed resources (OpenMetadataService, IngestionPipeline, etc.) can
              reference the same OpenMetadataConnection by name, avoiding repetition of
              target configuration across resources.
            properties:
              authSecretRef:
                description: |-
                  AuthSecretRef points to the Kubernetes Secret containing the JWT token
                  used to authenticate with the OpenMetadata API.
                properties:
                  key:
                    description: Key within the Secret's data field.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                  namespace:
                    description: |-
                      Namespace of the Secret. Required for cross-namespace access (e.g. reading
                      a secret from the openmetadata namespace).
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              url:
                description: URL is the base URL of the OpenMetadata API (e.g. "http://openmetadata.openmetadata:8585/api").
                minLength: 1
                type: string
            required:
            - authSecretRef
            - url
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
